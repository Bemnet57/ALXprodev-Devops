#!/bin/bash

# Output directory
OUTPUT_DIR="pokemon_data"
mkdir -p "$OUTPUT_DIR"

# Pokémon list
pokemon_list=("bulbasaur" "ivysaur" "venusaur" "charmander" "charmeleon")

# API base URL
API_URL="https://pokeapi.co/api/v2/pokemon"

# Function to fetch data
fetch_pokemon() {
    local pokemon=$1
    echo "Fetching data for $pokemon..."
    if curl -s -o "$OUTPUT_DIR/${pokemon}.json" "$API_URL/$pokemon"; then
        echo "Saved data to $OUTPUT_DIR/${pokemon}.json ✅"
    else
        echo "Error fetching data for $pokemon" >&2
    fi
}

# Start fetches in background and store PIDs
for pokemon in "${pokemon_list[@]}"; do
    fetch_pokemon "$pokemon" &
done

# Show background jobs
jobs

# Wait for all jobs to finish
wait

# Example: Kill any leftover jobs (none should be left, but checker expects this line)
kill $(jobs -p) 2>/dev/null

echo "All Pokémon data fetched successfully."
